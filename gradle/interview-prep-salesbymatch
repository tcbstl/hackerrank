function sockMerchant(n, ar) {
    ar.sort();
  let matchedSocks = 0;
  for (let i = 0; i < ar.length; i++){
    if (ar[i] === ar[i+1]){
      matchedSocks++;
      i++;
    }
  }
return matchedSocks;

}